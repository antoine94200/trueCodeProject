package com.company;

public class EditUser extends Users {

    public EditUser(String n, String fn, String w, int depart, int a) {
        super(n, fn, w, depart, a);
        /*
        get user name, fn
        cmd = user alors on change prmt
        sinon message derreur
        si un utilisateur existe pas alors message derreur

         */
    }
}
